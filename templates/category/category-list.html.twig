
<nav class="navbar navbar-expand-lg">
    <a class="navbar-brand" href="#">Categories</a>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            {% for catogry in categories %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ catogry.id }}">{{ catogry.title}}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
</nav>