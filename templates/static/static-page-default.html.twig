{% extends "layouts/default.html.twig" %}

{% block content %}

    <div class="container my-5">
    <h1>{{ pimcore_input('title') }}</h1>
    <h2>{{ pimcore_input('subTitle') }}</h2>

    <div class="col-lg-8 px-0">
        <div class="fs-5">
            {{ pimcore_areablock('content_block') }}
        </div>
    </div>

{% endblock %}